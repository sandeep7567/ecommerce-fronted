import Search from "@/components/search/Search";
import { FC } from "react";

interface SearchPageI {
  
};

const SearchPage: FC<SearchPageI> = ({}) => {
  return (
    <Search />
  )
}

export default SearchPage;